#/bin/bash

context=`kubectl config current-context`

cluster="$context"

if [ "$context" == "shared" ]; then
  cluster="shared-services"
fi
if [ "$context" == "devops" ]; then
  cluster="dv-devops-eks"
fi

aws eks get-token --cluster-name $cluster | jq -r .status.token | pbcopy
